{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "./mvnw spring-boot:run",
    "build": "./mvnw clean package",
    "clean": "./mvnw clean && rimraf target .turbo logs",
    "test": "./mvnw test",
    "install": "./mvnw install",
    "lint": "eslint .",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {}
}
